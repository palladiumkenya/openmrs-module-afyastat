{
  "icon": "icon-form-general",
    "title": "Contact follow up",
    "context": {
        "place": false,
        "person": true,
        "expression": "(user.role === 'hts_provider' || user.role === 'clinician') && !contact.muted  && contact.contact_type =='patient_contact' && (!contact.ipv_outcome || contact.ipv_outcome ==='No') && (summary.recentHtsTracing.phoneTraceOutcome != 'contactedAndLinked' && summary.recentHtsTracing.phoneTraceOutcome != 'contactedNotLinked')"
    }
}
